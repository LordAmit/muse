image: java:latest

stages:
  - build
  - test
  - deploy

build:
  stage: build
  tags:
    - maven
  script:
    - echo "Building Muse with Maven"
    - mvn -B compile --file code/Muse/pom.xml

test:
  stage: test
  tags:
    - maven
  script:
    - mvn test